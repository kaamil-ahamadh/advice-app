import React from "react";
import Loading from "../components/Loading";

const LoadingScreen = () => {
  return (
    <div className="app">
      <Loading />
    </div>
  );
};

export default LoadingScreen;
